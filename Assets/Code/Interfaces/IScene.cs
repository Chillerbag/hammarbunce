using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// TODO - implement. this is for sceneBuilder to build the different scenes. I guess Scenes will be a class? 
// what would this class include? A list of objects to instantiate, what the next scene is, how to transistion there? 
public class Level : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
